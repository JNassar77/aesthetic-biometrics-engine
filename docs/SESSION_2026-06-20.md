# Session-Status 2026-06-20

**Frontend-MVP-Foundation (Richtung A) gebaut + verifiziert** auf Branch `feat/frontend-mvp`
(3 Commits → PR). Engine **v2.2.0**, 503 Tests unverändert (diese Session: Deploy-Config,
Edge Function, Migration, Doku — kein Engine-Code). Vorherige Session siehe
`docs/SESSION_2026-06-17.md`.

## 🟢 DEPLOYED LIVE — 2026-06-20 (nach dem Handover, in dieser Session)
Die ganze Kette ist **live in Produktion** + end-to-end verifiziert:
- **Engine** auf Hetzner unter **`https://biometrics.novasyn.de`** (Docker-Container
  `aesthetic-biometrics-engine` auf `127.0.0.1:8003`, Caddy Auto-TLS, `supabase_connected:true`).
  Nicht-invasiv neben browser/api/admin/klarbefund (alle weiter bedient, geprüft).
- **Edge-Proxy** `engine-proxy` (v2) live; Secret `ENGINE_API_KEY` via Management-API gesetzt
  (Defaults für ENGINE_URL/ORG_ID im Code). Service-Key (`sb_secret_…`) in der Engine-`.env.production`.
- **Voller Lauf mit den 4 echten Fotos ÜBER den Proxy:** `/assessment` 200 (score 89.4, 13
  Overlay-Zonen, 3 Views, Profil korrekt abgelehnt) → **Persistenz** (assessments-Zeile `completed`
  + 4 Bilder im `patient-images`-Bucket unter `{org}/{assessment}/`) → `/report` 200 (gültiges PDF).
- Deploy ausgeführt von Claude per SSH (`hetzner` alias, Port 2222) + IONOS-MCP (DNS) +
  Supabase-Management-API (Secret). Artefakte in `deploy/hetzner/`.
- **Noch offen für echten Live-Betrieb:** nur die **Lovable-UI** generieren (Spec:
  `docs/FRONTEND_MVP_LOVABLE_SPEC.md`) + `ALLOWED_ORIGIN` der Function auf die Lovable-Domain.
  Gate 0 (DSGVO Art. 9) bleibt vor echten Patienten.

---

## ▶️ Prompt für die nächste Session (einfügen)

> Wir arbeiten weiter an der **Aesthetic Biometrics Engine** (medizinisches KI-Backend für
> Gesichtsbiometrie). Lies zuerst `docs/SESSION_2026-06-20.md` + `docs/SESSION_2026-06-17.md`
> und die Memory. Stand: **Frontend-MVP-Foundation steht + ist verifiziert** (Branch
> `feat/frontend-mvp` / PR): Engine-Deploy-Scaffold für **Hetzner** (`deploy/hetzner/`, lokal
> gebaut+`/health` grün), **Supabase Edge-Function `engine-proxy` deployed+live-verifiziert**
> (sicherer Proxy, hält den Engine-Key serverseitig), Default-Org geseedet, **Lovable-Build-Spec**
> (`docs/FRONTEND_MVP_LOVABLE_SPEC.md`), und die **MVP-Kette end-to-end auf den 4 echten Fotos
> verifiziert** (assessment→overlay→PDF; Profil korrekt abgelehnt). `.venv` (Py 3.14), Docker lokal.
>
> **Nächste Schritte (brauchen Jihad-Input → siehe „Go-Live-Gates" unten):**
> 1. **Engine live auf Hetzner** (`biometrics.novasyn.de` → `188.245.150.15`, Caddy/Docker, Port
>    8003): Service-Role-Key + Ausführungsmodus (SSH vs. Runbook) + DNS-Go + RAM-Check.
> 2. **Edge-Function-Secrets** setzen (`ENGINE_URL`/`ENGINE_API_KEY`/`ENGINE_ORG_ID`), dann
>    end-to-end über den Proxy testen.
> 3. **UI in Lovable** generieren (Spec ist fertig) → `ALLOWED_ORIGIN` auf die Lovable-Domain.
>
> Danach optional **Sprint 11** (klinische Validierung / 3D-Schwellen-Kalibrierung — braucht
> Caliper-Daten) und **Gate 0** (MDR + DSGVO Art. 9 DPIA) vor echten Patienten. Halte dich an
> `CLAUDE.md` + Freshness-Regel (Context7/MCP). Qualität vor Tempo. Frag bei Richtungsentscheidungen.

---

## Was diese Session gemacht wurde

### Entscheidungen (aus dem Q&A festgezurrt)
- **Persistenz JETZT an** (nicht ephemer) — aber **DEV/TEST**-Posture: nur Jihad + einwilligende
  Freunde, DSGVO-Pflicht erst zu Gate 0 / Produktion. Multi-tenant + RLS bleibt sauber gebaut.
- **Hosting = eigener Hetzner-Server, NICHT Railway** (`warum railway` — eigene EU-Infra =
  bessere Datenhoheit für Biometrie). Domain via **IONOS** (`novasyn.de`).
- **Bauumfang:** ich (Senior) baue Fundament + Lovable-Spec; Lovable macht die UI.

### Korrektur zur 06-17-Doku
- **Supabase `mbwteypkehrmeqzdzdph` ist `ACTIVE_HEALTHY`** (war als INACTIVE notiert → kein
  Restore nötig). Schema deckungsgleich, **RLS auf allen Tabellen**, 5 Alt-Migrations + 1 neue
  Seed-Migration, `patient-images`-Bucket existiert (privat, 10 MB, jpeg/png/webp).

### Gebaut (alles committet, branch `feat/frontend-mvp`)
1. **Engine-Deploy-Scaffold** `deploy/hetzner/` — nicht-invasiv neben den Live-Diensten
   (`browser`:8000/`sdk-agent`:8001/`klarbefund`): `docker-compose.yml` (Bind `127.0.0.1:8003`→
   Container `:8000`, mem-cap, swap off, healthcheck, no-new-privileges), `Caddyfile.snippet`
   (`biometrics.novasyn.de`→:8003, Auto-TLS), `.env.production.example`, `README.md` (Runbook +
   Pre-flight + Rollback). **Lokal verifiziert:** Docker-Build (Modell-SHA ok) + `/api/v2/health`
   `healthy, model_loaded true`.
2. **Edge Function `engine-proxy`** `supabase/functions/engine-proxy/` (Deno) — `verify_jwt=true`,
   **Tenant serverseitig injiziert** (Browser wählt Org nicht), Routes `POST /assessment`
   (multipart→Engine), `POST /report` (JSON→PDF), `GET /health`; streamt die Engine-Antwort durch.
   **Deployed (v1 ACTIVE) + live verifiziert:** anon→`503 proxy_misconfigured`, ohne Auth→`401`.
3. **Seed-Migration** `20260620110801_seed_default_organization` — Org „Praxis Nassar",
   `id 80c55491-46b6-4b82-98dc-2719758b4372` = `ENGINE_ORG_ID`. Remote appliziert + ins Repo gespiegelt.
4. **Lovable-Build-Spec** `docs/FRONTEND_MVP_LOVABLE_SPEC.md` — Copy-paste-Prompt (Consent →
   geführte 4-Winkel-Aufnahme → Review → Ergebnis-Heatmap + PDF) + voller technischer Contract
   (Endpoints, Headers, FormData, AssessmentResponse-Subset, Overlay-Back-Transform-Mathe,
   Fehler/Quality-Handling, Bild-Cap ~1600px, MVP-vs-Phase-2).
5. **End-to-End auf den 4 echten Fotos verifiziert** (lokaler Container): `/assessment` 200 → 3
   Views (`frontal`/`oblique_left`/`oblique_right`; **Profil korrekt abgelehnt** wg. yaw −49°),
   score 89.4, calibration iris reliable, 3D `multi_view_3d` rms 2.68 mm, 14 Zonen/13 Overlay,
   warnings `[]`; `/report` 200 gültiges PDF.

## Architektur
```
Praxis-Tablet (Lovable Web-App)
  └─ Consent → geführte 4-Winkel-Aufnahme → POST (multipart, anon-key)
       └─► Supabase Edge Function `engine-proxy` (EU · hält Engine-X-API-Key)
             └─► Engine /api/v2/assessment  (Hetzner, biometrics.novasyn.de, Docker+Caddy+TLS)
                   ◄── AssessmentResponse (overlay + plan)  → Heatmap aufs Foto
       └─► `engine-proxy/report` ─► Engine /api/v2/report ─► PDF
   (Engine-Key NIE im Browser; Tenant serverseitig injiziert; Engine persistiert async → Supabase)
```

## Go-Live-Gates (brauchen Jihad)
1. **`SUPABASE_KEY`** (service-role, mbwteypkehrmeqzdzdph) → in die Engine-`.env.production`
   (Persistenz). Nur aus dem Dashboard holbar (nicht per MCP).
2. **Deploy-Ausführung:** SSH-Drive von dieser Maschine **oder** Jihad fährt das 5-Schritt-Runbook
   (`deploy/hetzner/README.md`). Box trägt Live-Dienste → bewusst gated.
3. **DNS:** `biometrics.novasyn.de` A → `188.245.150.15` (kann ich per IONOS-MCP auf Go setzen).
4. **RAM:** `free -h` auf der Box (Engine ~1–1.5 GB; sdk-agent reserviert bis 2 GB).
5. Nach Engine-live: Edge-Function-Secrets setzen (`ENGINE_URL`/`ENGINE_API_KEY`/`ENGINE_ORG_ID`),
   `ALLOWED_ORIGIN` auf die Lovable-Domain → end-to-end über den Proxy testen.

## Offen / Hinweise
- **Sprint 11** (klinische Validierung + 3D-Schwellen-Kalibrierung) weiterhin offen — braucht
  Caliper-Referenzdaten (n≥20). Tiefen bleiben bis dahin ehrlich `estimated`.
- **Gate 0** (MDR-Scope + DSGVO Art. 9 DPIA) bleibt der Engpass vor echten Patienten.
- Die 5 Alt-Migrations liegen nur in der Remote-History — für volle From-scratch-Reproduzierbarkeit
  später ins Repo exportieren.
- Frontal-Testfoto war leicht nicht-neutral (`NON_NEUTRAL_EXPRESSION`) → Capture-Hinweis „neutrale
  Mimik" steht in der Lovable-Spec.
