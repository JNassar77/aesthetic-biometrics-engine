# Session-Status 2026-06-17

Stand nach der Rückkehr-aus-der-Pause-Session. Branch: `chore/deps-update-v1-removal` → [PR #1](https://github.com/JNassar77/aesthetic-biometrics-engine/pull/1). CI **grün** (Tests + Docker). 458 Tests.

## Was erledigt wurde

**Wartung & Update**
- Alle Dependencies auf aktuellen Stand (FastAPI 0.137, Starlette 1.3 🔒 Security-Fix, mediapipe 0.10.35, supabase 2.31, pydantic 2.13). Lokale `.venv` (Python 3.14).
- V1-Legacy-Schicht entfernt (754 Zeilen) — mediapipe 0.10.35 hatte die `mp.solutions`-API entfernt, auf der V1 lief.

**Strategie-Review** (`docs/STRATEGY_REVIEW_2026-06.md`) → Kurs halten, Härtung statt Pivot. Umgesetzt:
- Ops: Threadpool, Landmarker-Singleton, Auth fail-closed (Prod), Modell-SHA-Pin.
- Ehrlichkeit: relative-z-/Out-of-plane-Werte als `estimated` gekennzeichnet, Kalibrierungs-Sicherheits-Gate.

**3 Produktions-Bugs durch das Update aufgedeckt + gefixt**
1. mediapipe `mp.solutions` weg → V1 entfernt.
2. mediapipe 0.10.35 braucht `libGLESv2`/`libEGL` → in CI + Dockerfile ergänzt (hätte Container-Start gecrasht).
3. `python:3.11-slim` ist auf Debian trixie gewandert (kein `libgl1-mesa-glx`) → Base-Image auf `slim-bookworm` gepinnt + `libgl1`.

**3D-Tiefe (Premium-Feature)**
- `docs/CONCEPT_3D_DEPTH.md` — Konzept (Triangulation aus 3 Views, Tiefensensor, 3DMM).
- `app/analysis/multiview_reconstruction.py` — metrische Landmark-Triangulation (orthografische Faktorisierung mit bekannten Rotationen, Iris fixiert die Skala). Deterministisch, 8 synthetische Round-Trip-Tests (exakt).
- HEIC/iPhone-Bildsupport (`pillow-heif`).

**Kritischer vorbestehender Bug gefunden (durch echte Fotos)**
- `head_pose.py` vertauschte **yaw und pitch** (ZYX statt YXZ). Kopfdrehungen wurden als pitch gemeldet → quality_gate lehnte oblique/profil ab → Multi-View funktionierte auf echten Aufnahmen **nie**. Synthetische Tests bestätigten den Bug selbst (Helper nutzte dieselbe falsche Konvention).
- Gefixt mit YXZ (scipy), gegen echte Posen verifiziert.

**3D an echten Daten validiert** (4 Testfotos, `test_images/` — gitignored)
- Nach dem Fix: obliques akzeptiert + kalibriert, 3D-Rekonstruktion läuft.
- **Pupillenabstand rekonstruiert zu 61,9 mm** (anatomische Norm 58–66) → metrische Skala bestätigt.
- Iris-Breite 13,1 mm (Norm 11,7; ~12 % Überschätzung), reproj_rms 2,7 mm → erwarteter orthografischer Näherungsfehler bei hand-gehaltenen Fotos.

## Offen — nächste Schritte (Ziel: „voller Funktionsumfang, deployment-bereit", DSGVO/Regulatorik separat vor Go-Live)

**Task 10 — Engines auf echte 3D umstellen** (vorbereitet, z-Konvention geklärt)
- `volume_engine`/`profile_engine`: Tiefen aus `multiview_reconstruction` statt relativer z.
- **z-Konvention: +z = vorne** (Nase +47, Wangen −80 mm). Die Engines erwarten die umgekehrte (negativ=vorne) → **3D-z beim Anbinden negieren**, sonst invertieren `is_hollowed`/`is_flattened`.
- Orchestrator baut `ViewObservation`s (landmarks_px, R aus transformation_matrix orthonormalisiert, px_per_mm) → `reconstruct_3d` → an die Engines + in den Response (`reconstruction`-Block: n_views, spread, reproj_rms).
- Architektur: aktuell 3 feste View-Slots (frontal/oblique/profil, nur EINE oblique). Auf **beidseitige obliques** (45°L + 45°R) erweitern — verbessert 3D UND die bestehende Symmetrie-/Volumenanalyse.
- **Wichtig:** Die klinischen Schwellen (z.B. HOLLOW_THRESHOLD=3 mm) sind für die alten Pseudo-Werte gesetzt. Für echte 3D-mm **nicht kalibriert** → Tiefen bleiben `estimated`, bis gegen echte Referenzdaten kalibriert (= Sprint 11). NICHT blind auf `estimated=False` setzen.

**Task 11 — Klinischer PDF-Report** pro Assessment (Zonen, Severity, Plan, Messwerte mit estimated-Kennzeichnung, Aesthetic Score).

**Task 12 — Frontend-Daten**: Injection-Point-Koordinaten pro Zone (Overlay) + Zone-Heatmap-Export.

**Task 13 — Railway-Deployment-Config**: `railway.toml` (eu-Region), `.env.production`-Vorlage (ohne DSGVO-Inhalt), Modell-Download im Build.

**Sprint 11 (klinische Validierung, braucht echte Referenzdaten)**
- Bland-Altman gegen Caliper für die ~5–8 horizontalen Frontalmaße.
- Schwellen-Kalibrierung für die 3D-Tiefenmaße.
- Perspektivische Verfeinerung der 3D (Bundle Adjustment) — orthografische Näherung quantifizieren/korrigieren.

## Aufnahme-Protokoll (für gute 3D-Ergebnisse, mit Jihad geklärt)
Ideal sind **4 Aufnahmen**: frontal 0°, 45° links, 45° rechts, 90° Profil. Die beidseitigen 45° tragen die 3D-Triangulation (Überlappung, beide Wangen); das 90°-Profil liefert die klinischen Profil-Linien (E-Linie, Nasolabial-, Kinnwinkel). Standardisiert (Stativ, neutrale Mimik, Augen sichtbar) → ~1–2 mm erreichbar.
