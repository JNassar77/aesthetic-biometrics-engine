# Session-Status 2026-06-21

**Frontend-MVP-App gebaut + verifiziert, Engine-Overlay-Fix live deployed.** Branch
`feat/frontend-mvp` / PR #3. Engine **v2.2.0**, **503 Tests grün**. Vorherige Sessions:
`docs/SESSION_2026-06-20.md` (Go-live) + `docs/SESSION_2026-06-17.md` (3D/Reports).

## 🟢 Was diese Session erreicht wurde

1. **Aesthetic Scan Frontend** (`frontend/`, Vite 8 / React 19 / TS 6) — dünne, geführte
   Web-App, spricht **nur** mit der Edge Function `engine-proxy` (nie direkt mit der Engine,
   hält nie den Engine-Key): Consent → **geführte 4-Winkel-Aufnahme** (Live-Kamera **+ pro Shot
   Datei-Upload-Fallback** — Desktop & Wiederverwendung der Stativ-Fotos) → Review → **Ergebnis**
   (Heatmap aufs Original-Foto rückgerechnet, Aesthetic Score, Top-Zonen, Kontraindikationen,
   Quality/Calibration-Banner, **PDF-Download**). Ruhiges klinisches Design, Teal-Akzent.
   Prod-Build sauber (`npm run build`, ~66 kB gzip JS).

2. **Echter Bug gefunden + gefixt** (genau wofür „verifiziert bauen" da ist): Das Overlay war
   unter dem **kanonischen** View-Namen `oblique` verschlüsselt — die Antwort sagte aber **nicht,
   welcher** physische Oblique (links/rechts) das ist. Heatmap auf der falschen Wange = alle
   Marker daneben. Fix: **`canonical_oblique_view`** additiv in die Overlay-Antwort
   (`overlay.py`, `orchestrator.py`, `schemas_v2.py`, `v2_routes.py`) + Test. Frontend mappt
   kanonisch→physisch; sicherer Fallback lässt die Oblique-Heatmap weg, wenn mehrdeutig (beide
   Obliques, alte Engine), statt die falsche Wange zu malen. **503 Tests grün.**

3. **Engine neu deployed auf Hetzner** (Owner-Go): `git pull` + `docker compose up -d --build`
   auf der Box (`/root/aesthetic-biometrics-engine`, branch `feat/frontend-mvp` @ `43ff59a`),
   nicht-invasiv (eigener Container :8003), Container **healthy**. **Live verifiziert:** die
   Antwort über den Proxy enthält jetzt `canonical_oblique_view: "oblique_left"` → die
   Oblique-Heatmap erscheint live aufs korrekte Foto.

4. **Verifikation** (lokal, gegen den LIVE-Proxy mit den 4 echten Fotos):
   - `frontend/scripts/smoke-proxy.mjs` (committet): `/assessment` 200 (Score 89.4, Profil
     korrekt abgelehnt, Kalibrierung reliable, 0 Warnings, 13 Overlay-Zonen), `/report` gültiges PDF.
   - UI gegen die Live-Antwort gerendert (Preview): Results zeigt Score 89, Frontal-Heatmap
     (9 Zonen) + Oblique-Heatmap (4 Zonen); Heatmap-Marker liegen anatomisch korrekt
     (Canvas-Pixel-Analyse: frontal Brauen→Lippen, oblique Wange/Temporal). `tsc` grün.

5. **DOD-Nacharbeit**: REGISTRY (Railway→**Hetzner**, `engine-proxy`, Frontend, neue Env-Vars,
   `canonical_oblique_view`, 503 Tests), FEATURES (F-130/131/132 Delivery), GRAPH (Frontend-
   Delivery-Diagramm), TASKS (Sprint 14). Caliper-SOP (`docs/clinical/`) ins Repo.

## ▶️ Prompt für die nächste Session (einfügen)

> Wir arbeiten weiter an der **Aesthetic Biometrics Engine** (medizinisches KI-Backend für
> Gesichtsbiometrie). Lies zuerst `docs/SESSION_2026-06-21.md` + `docs/SESSION_2026-06-20.md`
> und die Memory. **Stand: voller Stack LIVE + verifiziert.** Engine v2.2.0 auf **Hetzner**
> (`https://biometrics.novasyn.de`, Docker :8003 + Caddy TLS), Edge Function `engine-proxy` live,
> **Frontend-App `frontend/`** (Vite/React/TS) gebaut + end-to-end gegen den Live-Proxy verifiziert
> (Capture → Heatmap aufs Foto → PDF). Overlay-Fix `canonical_oblique_view` deployed → Oblique-
> Heatmaps live korrekt. **503 Tests grün.** `.venv` (Py 3.14), SSH-Alias `hetzner` (:2222),
> Box-Repo `/root/aesthetic-biometrics-engine`.
>
> **Nächste Schritte:**
> 1. **Frontend hosten** — statisches `frontend/dist/` (Caddy auf der Box als eigener Block, oder
>    Vercel). Danach **`ALLOWED_ORIGIN`** der Edge Function auf die Frontend-Domain (CORS-Härtung).
> 2. **Box auf `main`** umstellen (nach PR-#3-Merge): auf der Box `git checkout main && git pull`
>    + `docker compose … up -d --build` (Code identisch, nur Branch-Hygiene).
> 3. **Sprint 11** (klinische Validierung): die Caliper/Foto-**SOP liegt jetzt in
>    `docs/clinical/`** — Referenzdaten (n≥20) erheben → 3D-Tiefen-Schwellen kalibrieren
>    (HOLLOW/BREAK/Ogee) → gezielt `estimated=False`; Bland-Altman.
> 4. **Gate 0** (MDR-Scope + DSGVO Art. 9 DPIA) bleibt der Engpass vor echten Patienten.
>
> Halte dich an `CLAUDE.md` + Freshness-Regel (Context7/MCP). Qualität vor Tempo. Frag bei
> Richtungsentscheidungen.

## Offen / Hinweise

- **Test-Daten:** diese Session hat über die Smoke-Runs **~3 weitere Test-Assessments** in
  Supabase erzeugt (Jihads Fotos, eingewilligt; assessments-Zeilen + Bilder in `patient-images`).
  Bei Bedarf leeren.
- **Box läuft auf `feat/frontend-mvp`** (= Stand `43ff59a`). Nach dem Merge identisch zu `main`;
  Branch-Umstellung ist Kosmetik (Schritt 2 oben).
- **Frontend ist noch nicht gehostet** — lokal `npm run dev` / `npm run build`. `ALLOWED_ORIGIN`
  steht noch auf `*` (dev) bis die Domain bekannt ist.
- **DEV/TEST-Posture** bleibt: einwilligende Test-Personen, kein echter Patient vor Gate 0.
