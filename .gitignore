__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
# Env files with real secrets — ignore all, keep the *.example templates
.env
.env.*
!.env.example
!.env.production.example
.venv/
venv/
*.so
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
.coverage
.pytest_cache/
htmlcov/
# Local face photos — biometric data, never commit (keep the README)
test_images/*
!test_images/README.md
# ML models (download via curl, see CLAUDE.md)
models/*.task
models/*.tflite
